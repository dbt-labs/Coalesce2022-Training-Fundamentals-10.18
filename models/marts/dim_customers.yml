version: 2

models:
  - name: dim_customers
    description: Customer and order information combined
    columns:
      - name: customer_id
      - name: first_name
      - name: last_name
      - name: first_order_date
      - name: most_recent_order_date
      - name: number_of_orders